# Module `lists`

<a name='module-lists'></a>
*Generated on 2025-10-18T20:08:17*

## Function **first_after_last_digits**

<a name='lists-function-first_after_last_digits'></a>
```python
first_after_last_digits(strings)
```

Return the `str` directly after the last `str` containing only digits
in `strings`.

